# security-controls-mapper
CLI that maps internal security controls (from YAML input) to common frameworks such as NIST CSF and CIS Controls. The tool outputs a coverage matrix in CSV and Markdown formats.
